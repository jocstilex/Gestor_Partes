import 'package:flutter/material.dart';

class EstadisticasAlumnoPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Datos del alumno'),
        centerTitle: true,
      ),
    );
  }
}
